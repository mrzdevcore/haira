[package]
name = "haira-runtime"
version = "0.1.0"
edition = "2021"
description = "Runtime library for compiled Haira programs"
license = "Apache-2.0"

[workspace]

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
libc = "0.2"
regex = "1"
lazy_static = "1.4"

[features]
default = []
