// Example 08: Structs and methods
import "io"

struct User {
    name: string,
    age: int
}

User.greet() -> string {
    return "Hello, I'm ${self.name}"
}

fn main() {
    user = User{ name = "Alice", age = 30 }
    io.println(user.name)
    io.println(user.greet())
}
