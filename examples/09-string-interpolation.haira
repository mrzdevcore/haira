import "io"

fn greet(name: string, age: int) -> string {
    return "Hello, {name}! You are {age} years old."
}

fn main() {
    msg = greet("Alice", 30)
    io.println(msg)

    lang = "Haira"
    version = 1
    io.println("Welcome to {lang} v{version}!")
}
